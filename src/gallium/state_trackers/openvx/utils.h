/*
 * Almost everithing here can be found in mesa source tree.
 * Include some hearders later to get ride of this.
 */

#define max(a,b) a > b ? a : b;

#define CALLOC(a) (calloc(a))
#define FREE(a)   (free(a));